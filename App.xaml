<Application
    x:Class="MineModMapSelector.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MineModMapSelector">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- WinUI-Stilressourcen -->
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />

            </ResourceDictionary.MergedDictionaries>

            <!-- Stil f�r Hintergrund -->
            <Style x:Key="PageBackgroundStyle" TargetType="Page">
                <Setter Property="Background">
                    <Setter.Value>
                        <ImageBrush ImageSource="Assets/MinecraftBackground.png" Stretch="UniformToFill" Opacity="0.4" />
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Stil f�r Buttons -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#4CAF50" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Height" Value="50" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="10,5" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="Margin" Value="0,10,0,10" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>